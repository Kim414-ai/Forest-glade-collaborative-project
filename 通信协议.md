---
AIGC:
    Label: "1"
    ContentProducer: 001191110102MACQD9K64018705
    ProduceID: 3524394239674633_0-drive/227364537316493328/森林空地协同巡检系统_通信协议说明书.md
    ReservedCode1: ""
    ContentPropagator: 001191110102MACQD9K64028705
    PropagateID: 3524394239674633#1789897636222
    ReservedCode2: ""
---
# 《森林空地协同巡检系统 通信协议说明书》

**版本**：v1.0
**编制**：彭浩宇
**日期**：2026年9月
**项目**：森林空地协同巡检项目（北京交通大学 · 电气工程学院）

---

## 一、文档目的

本协议用于规定系统内三个角色之间的数据通信格式，确保无人机端、控制中心、无人车端的程序能够正确交换信息，实现"空中发现—地面核查—结果回传"的完整闭环。

三个开发人员按本协议编写代码，可保证各模块能够互联互通。

---

## 二、系统角色

| 角色 | 职责 |
|:--|:--|
| 无人机端 | 巡航采集图像，检测异常目标，上报异常信息 |
| 控制中心 | 接收无人机数据，下发任务给无人车，汇总巡检报告 |
| 无人车端 | 接收任务，自主导航至目标点，近检采集并回传结果 |

---

## 三、通信方式

| 项目 | 说明 |
|:--|:--|
| 传输介质 | Wi-Fi（主）/ 串口（备用） |
| 数据格式 | JSON 文本 |
| 传输协议 | 无人机→控制中心：UDP（实时性优先）；控制中心→无人车：TCP（可靠性优先） |
| 编码 | UTF-8 |

---

## 四、消息类型定义

系统共定义 **3 种消息类型**：

### 4.1 异常告警消息（无人机 → 控制中心）

当无人机检测到异常目标时发送。

```json
{
  "msg_type": "abnormal_alert",
  "target_id": "T001",
  "category": "smoke",
  "confidence": 0.87,
  "coords": {
    "lat": 39.9512,
    "lng": 116.3984
  },
  "image_url": "http://xxx/snap_001.jpg",
  "timestamp": "2026-07-10 14:30:00"
}
```

**字段说明：**

| 字段 | 类型 | 说明 |
|:--|:--|:--|
| msg_type | string | 消息类型，固定为 "abnormal_alert" |
| target_id | string | 目标编号，格式 T+3位数字 |
| category | string | 异常类别：smoke / fire / person / vehicle |
| confidence | float | 置信度，0~1 |
| coords.lat | float | 纬度 |
| coords.lng | float | 经度 |
| image_url | string | 异常画面截图地址 |
| timestamp | string | 发生时间 |

---

### 4.2 任务下发消息（控制中心 → 无人车）

控制中心确认异常后，向无人车下发近检任务。

```json
{
  "msg_type": "mission_dispatch",
  "target_id": "T001",
  "coords": {
    "lat": 39.9512,
    "lng": 116.3984
  },
  "mission_type": "close_inspection",
  "priority": "high",
  "timestamp": "2026-07-10 14:30:05"
}
```

**字段说明：**

| 字段 | 类型 | 说明 |
|:--|:--|:--|
| msg_type | string | 固定为 "mission_dispatch" |
| target_id | string | 目标编号，与告警消息一致 |
| coords | object | 目标经纬度坐标 |
| mission_type | string | 任务类型，固定为 "close_inspection" |
| priority | string | 优先级：high / normal / low |
| timestamp | string | 下发时间 |

---

### 4.3 结果回传消息（无人车 → 控制中心）

无人车完成近检后回传结果。

```json
{
  "msg_type": "result_report",
  "target_id": "T001",
  "status": "confirmed",
  "photo_url": "http://xxx/close_001.jpg",
  "note": "确认烟雾，疑似火情",
  "timestamp": "2026-07-10 14:35:20"
}
```

**字段说明：**

| 字段 | 类型 | 说明 |
|:--|:--|:--|
| msg_type | string | 固定为 "result_report" |
| target_id | string | 目标编号 |
| status | string | 状态：confirmed（确认异常）/ normal（误报） |
| photo_url | string | 近检照片地址 |
| note | string | 备注说明 |
| timestamp | string | 完成时间 |

---

## 五、完整通信流程示例

```
① 无人机巡航，检测到烟雾
   │
   ▼
② 无人机 → 控制中心：发送 abnormal_alert
   {"category": "smoke", "coords": {39.9512, 116.3984}, ...}
   │
   ▼
③ 控制中心接收，显示异常并确认
   │
   ▼
④ 控制中心 → 无人车：发送 mission_dispatch
   {"target_id": "T001", "coords": {39.9512, 116.3984}, ...}
   │
   ▼
⑤ 无人车导航至目标点，近检拍照
   │
   ▼
⑥ 无人车 → 控制中心：发送 result_report
   {"status": "confirmed", "note": "确认烟雾", ...}
   │
   ▼
⑦ 控制中心生成巡检报告（时间 / 地点 / 结果 / 建议）
```

---

## 六、异常情况处理

| 情况 | 处理方式 |
|:--|:--|
| 无人机漏检 | 定期返回巡航状态心跳包 |
| 通信中断 | 超时3秒重发，最多3次 |
| 无人车无法到达 | 回传 status = "failed" + 原因说明 |
| 误报 | 回传 status = "normal"，记录误报位置 |

---

## 七、后续扩展（预留）

- 支持多目标批量告警
- 支持无人机实时视频流推送
- 支持多种传感器数据（红外、温度）

---

> 本内容由 Coze AI 生成，请遵循相关法律法规及《人工智能生成合成内容标识办法》使用与传播。
